# Database - Prisma Postgres
# รับค่าจาก Prisma Data Platform หรือ Vercel Dashboard > Storage

# สำหรับ queries
POSTGRES_URL="postgres://..."

# สำหรับ migrations และ direct access
POSTGRES_URL_NON_POOLING="postgres://..."

# Optional - ถ้าใช้ Prisma Accelerate (caching/connection pooling)
# PRISMA_DATABASE_URL="prisma+postgres://..."

# Development (SQLite - comment out when deploying)
# DATABASE_URL="file:./dev.db"

# Authentication
# ตั้งรหัสผ่านสำหรับป้องกันไม่ให้คนอื่นเข้าถึง (ไม่ต้องใส่ใน dev)
# AUTH_PASSWORD="your-secret-password"
