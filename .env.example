# ============================================
# Database Configuration
# ============================================

# เลือก source ที่ต้องการใช้ (สำหรับ reference เท่านั้น)
DB_SOURCE="neon-postgres"

# **สำคัญ**: ตั้งค่า DATABASE_URL และ DIRECT_URL ตาม DB_SOURCE ที่เลือก
# เปลี่ยนแค่ comment/uncomment บรรทัดที่ต้องการใช้

# --------------------------------------------
# Option 1: Neon Postgres (แนะนำ - Free tier ใจกว้างกว่า)
# --------------------------------------------
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"

# --------------------------------------------
# Option 2: Prisma Postgres / Vercel Postgres
# --------------------------------------------
# DATABASE_URL="postgres://..."
# DIRECT_URL="postgres://..."

# ============================================
# เก็บค่าต้นฉบับไว้ (optional - สำหรับ reference)
# ============================================

# Prisma Postgres
POSTGRES_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."

# Neon Postgres (รับค่าจาก https://neon.tech)
NEON_DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
NEON_DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"

# ============================================
# Authentication
# ============================================
# ตั้งรหัสผ่านสำหรับป้องกันไม่ให้คนอื่นเข้าถึง (ไม่ต้องใส่ใน dev)
# AUTH_PASSWORD="your-secret-password"
